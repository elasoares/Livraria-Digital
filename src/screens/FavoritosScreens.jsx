import { Text, View } from "react-native";

const FavoritosScreens = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>FavoritosScreens</Text>
    </View>
  );
};

export default FavoritosScreens;
